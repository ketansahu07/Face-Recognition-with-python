This directory must contain subdirectories each named as the name of the person that subdirectory would be storing.

The images stored in the subdirectories should be of the person alone in each image, i.e it should have images with more than one person.